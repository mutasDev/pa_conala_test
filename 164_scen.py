#Find last occurrence of character '}' in string "abcd}def}"


The code for this program is as follows:

str = "abcd}def}"

last_occurrence = str.rfind("}")

print("The last occurrence of '}' is at index", last_occurrence)