#create array containing the maximum value of respective elements of array `[2, 3, 4]` and array `[1, 5, 2]`


```
import numpy as np

a = np.array([2, 3, 4])
b = np.array([1, 5, 2])

c = np.maximum(a, b)

print(c)
```